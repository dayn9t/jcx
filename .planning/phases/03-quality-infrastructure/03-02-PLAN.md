---
phase: 03-quality-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QLTY-02
user_setup: []

must_haves:
  truths:
    - "CI runs on every push to main"
    - "CI runs on every pull request to main"
    - "CI runs ruff lint check"
    - "CI runs ruff format check"
    - "CI runs tests with coverage"
    - "CI fails if coverage below 80%"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI/CD pipeline"
      contains: "name: CI"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "uv"
      via: "astral-sh/setup-uv@v5"
      pattern: "setup-uv"
    - from: ".github/workflows/ci.yml"
      to: "pytest"
      via: "pytest --cov"
      pattern: "pytest.*--cov"
---

<objective>
Create GitHub Actions CI/CD pipeline that runs on every push and PR, enforcing linting, formatting, and coverage thresholds.

Purpose: Automate quality enforcement in the cloud, catching issues before merge.
Output: .github/workflows/ci.yml with complete CI pipeline.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-infrastructure/03-RESEARCH.md

<interfaces>
<!-- Project uses uv as build backend -->
<!-- From pyproject.toml -->
```toml
[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

requires-python = "~=3.12.0"
```

<!-- Test command from research -->
Run: `uv run pytest --cov=jcx --cov-fail-under=80 -m "not integration"`

<!-- Ruff commands -->
Lint: `uv run ruff check`
Format: `uv run ruff format --check`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with the following structure:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --dev

      - name: Lint with ruff
        run: uv run ruff check

      - name: Format check with ruff
        run: uv run ruff format --check

      - name: Run tests with coverage
        run: uv run pytest --cov=jcx --cov-fail-under=80 -m "not integration"

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: htmlcov/
          retention-days: 7
```

Key decisions:
- Use astral-sh/setup-uv@v5 with caching for fast builds
- Python 3.12 to match project requirement
- Exclude integration tests (-m "not integration") as they may require external services
- Upload coverage artifact even on failure for debugging
</action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "name: CI" .github/workflows/ci.yml && echo "CI workflow created"</automated>
  </verify>
  <done>.github/workflows/ci.yml exists with lint, format, and test jobs</done>
</task>

</tasks>

<verification>
1. CI workflow file exists: `.github/workflows/ci.yml`
2. Workflow triggers on push to main
3. Workflow triggers on pull_request to main
4. Workflow runs ruff check
5. Workflow runs ruff format --check
6. Workflow runs pytest with coverage and fail_under=80
</verification>

<success_criteria>
- `.github/workflows/ci.yml` exists with valid YAML syntax
- Workflow includes all three quality gates: lint, format, coverage
- Uses uv for dependency management (matches project setup)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-infrastructure/03-02-SUMMARY.md`
</output>
