---
phase: 01-foundation-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/api/task/task_test.py
  - tests/api/task/test_task_db.py
  - tests/net/subscriber_test.py
autonomous: true
requirements:
  - FIX-03
user_setup: []
must_haves:
  truths:
    - "pytest runs without collection errors"
    - "MQTT test is skipped by default (marked as integration)"
    - "Test imports resolve to task_types module"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest configuration with integration marker"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "pytest marker registration"
      min_lines: 5
    - path: "tests/net/subscriber_test.py"
      provides: "Integration test marker on MQTT test"
      contains: "@pytest.mark.integration"
  key_links:
    - from: "pyproject.toml"
      to: "pytest"
      via: "[tool.pytest.ini_options] addopts"
      pattern: "addopts.*not integration"
---

<objective>
Configure pytest infrastructure and fix test collection issues.

Purpose: Enable reliable test execution with integration test markers, and fix import errors blocking test collection.
Output: Working pytest configuration, fixed imports, integration-marked MQTT test.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repair/01-CONTEXT.md
@.planning/phases/01-foundation-repair/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pytest with integration marker</name>
  <files>pyproject.toml, tests/conftest.py</files>
  <action>
Add pytest configuration to pyproject.toml:

```toml
[tool.pytest.ini_options]
addopts = "-m 'not integration'"
markers = [
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]
```

Create tests/conftest.py to register the marker (pytest requires explicit registration in newer versions):

```python
def pytest_configure(config):
    config.addinivalue_line(
        "markers", "integration: marks tests as integration tests"
    )
```

This ensures MQTT test is skipped by default but can be run with `pytest -m integration`.
  </action>
  <verify>
    <automated>pytest --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>pytest configuration added, integration marker registered, tests collect without errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix test import errors</name>
  <files>tests/api/task/task_test.py, tests/api/task/test_task_db.py</files>
  <action>
Fix broken imports in test files. The module was renamed from `task_db` to `task_types`:

In tests/api/task/task_test.py:
- Change `from jcx.api.task.task_db import TaskStatus` to `from jcx.api.task.task_types import TaskStatus`

In tests/api/task/test_task_db.py:
- Change `from jcx.api.task.task_db import StatusInfo, TaskDb, TaskInfo, TaskStatus` to `from jcx.api.task.task_types import StatusInfo, TaskInfo, TaskStatus`
- Note: TaskDb may need to be imported from a different module if it's not in task_types. Check if TaskDb exists in task_types.py (it doesn't based on code review). If TaskDb is defined elsewhere, find the correct import path.

After fixing, verify tests collect without ModuleNotFoundError.
  </action>
  <verify>
    <automated>pytest --collect-only tests/api/task/ 2>&1 | grep -E "(error|Error|collected)"</automated>
  </verify>
  <done>All test files in tests/api/task/ collect without import errors</done>
</task>

<task type="auto">
  <name>Task 3: Mark MQTT test as integration</name>
  <files>tests/net/subscriber_test.py</files>
  <action>
Add pytest integration marker to the MQTT subscriber test:

1. Add `import pytest` at the top of the file
2. Add `@pytest.mark.integration` decorator above the `demo_sub()` function

The root cause of the hang is `dispatch_msg()` using `loop_forever()` which blocks indefinitely. By marking as integration, it's skipped by default but can be run explicitly when a broker is available.

Do NOT change the test implementation - only add the marker.
  </action>
  <verify>
    <automated>pytest --collect-only tests/net/subscriber_test.py 2>&1 | grep -E "(integration|collected)"</automated>
  </verify>
  <done>MQTT test marked with @pytest.mark.integration, skipped by default</done>
</task>

</tasks>

<verification>
After all tasks:
1. `pytest --collect-only` shows no errors
2. `pytest -x -q` runs without hanging (MQTT test skipped)
3. `pytest -m integration --collect-only` shows subscriber_test.py collected
</verification>

<success_criteria>
- pytest configuration exists in pyproject.toml with integration marker
- tests/conftest.py exists with marker registration
- All test imports resolve correctly
- MQTT test skipped by default, runs only with `-m integration`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-01-SUMMARY.md`
</output>
