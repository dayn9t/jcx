---
phase: 04-type-safety-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/jcx/rs/*.py
  - src/jcx/util/*.py
  - src/jcx/sys/fs.py
  - src/jcx/time/*.py
  - src/jcx/text/*.py
  - src/jcx/db/*.py
  - src/jcx/net/*.py
autonomous: true
requirements:
  - TYPE-03
user_setup: []

must_haves:
  truths:
    - "All public modules can be imported without type errors"
    - "pyright --verifytypes reports no missing function annotations on public APIs"
    - "Public API type hints are complete and accurate"
  artifacts:
    - path: "src/jcx/rs/"
      provides: "Result/Option types (rustshed wrapper)"
    - path: "src/jcx/util/"
      provides: "Utility functions"
    - path: "src/jcx/sys/fs.py"
      provides: "File system operations"
    - path: "src/jcx/time/"
      provides: "Time/date utilities"
    - path: "src/jcx/text/"
      provides: "Text/JSON handling"
  key_links:
    - from: "pyright"
      to: "all public modules"
      via: "type verification"
      pattern: "uv run pyright --verifytypes jcx"
---

<objective>
Verify all public APIs have proper type hints using pyright's verifytypes mode. Fix any missing or incorrect type annotations.

Purpose: Ensure type checkers can verify code that uses jcx public APIs.
Output: Verified type hints on all public functions and classes.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-type-safety-documentation/04-RESEARCH.md

<interfaces>
<!-- Public API modules to verify -->

## jcx Public Modules Structure:
```
src/jcx/
├── rs/              # Result/Option types (rustshed wrapper)
├── util/            # Utility functions (algo, array, err, lict, logging_config, mutithread, oo, trace)
├── sys/fs.py        # File system operations
├── time/            # Time/date utilities (clock_time, calendar_type)
├── text/            # Text/JSON handling (txt_json, txt_json5, io)
├── db/              # Database abstractions (rdb/)
├── net/mqtt/        # MQTT messaging
├── api/             # REST API helpers (flask-restx)
└── bin/             # CLI tools (NOT public API)
```

## Note: bin/ directory contains CLI entry points, not public API
## Note: All __init__.py files are empty - no explicit public API exports
## Users import directly from submodules

## py.typed marker already exists at src/jcx/py.typed (PEP 561 compliance)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pyright verifytypes and collect missing annotations</name>
  <files>src/jcx/</files>
  <action>
    Run pyright's verifytypes mode to identify missing type annotations:

    ```bash
    uv run pyright --verifytypes jcx --outputjson 2>&1 | tee /tmp/pyright-verify.json || true
    ```

    Parse the output to identify:
    1. Functions/methods missing return type annotations
    2. Functions/methods missing parameter type annotations
    3. Classes missing type information

    Focus on truly public APIs (not bin/, not _private modules, not test files).
  </action>
  <verify>
    <automated>uv run pyright --verifytypes jcx 2>&1 | head -100</automated>
  </verify>
  <done>pyright verifytypes output captured and analyzed</done>
</task>

<task type="auto">
  <name>Task 2: Fix missing type annotations in public APIs</name>
  <files>src/jcx/rs/*.py, src/jcx/util/*.py, src/jcx/sys/fs.py, src/jcx/time/*.py, src/jcx/text/*.py</files>
  <action>
    Based on Task 1 output, add missing type annotations to public functions.

    Common patterns to apply:
    1. Add return type `-> None` for functions that don't return a value
    2. Add `-> str`, `-> int`, `-> bool`, etc. for simple return types
    3. Use `from typing import Optional` and `-> Optional[T]` for nullable returns
    4. Use `from collections.abc import Iterator` for generator functions
    5. Use generic types like `list[T]`, `dict[str, Any]` for container returns

    Priority order for fixes:
    1. rs/ module (core Result/Option types)
    2. sys/fs.py (file system operations)
    3. text/*.py (JSON handling)
    4. time/*.py (time utilities)
    5. util/*.py (utility functions)

    Skip internal functions (prefixed with _), test functions, and CLI entry points.
  </action>
  <verify>
    <automated>uv run pyright --verifytypes jcx 2>&1 | grep -c "missing" || echo "0 missing"</automated>
  </verify>
  <done>All public API functions have type annotations, pyright verifytypes shows no missing annotations</done>
</task>

</tasks>

<verification>
1. Run `uv run pyright --verifytypes jcx` - should report no missing type annotations for public APIs
2. Run `uv run pyright src/jcx` - should complete without errors
3. Verify imports work: `uv run python -c "from jcx.rs import Result, Option; from jcx.sys.fs import files_in; from jcx.text.txt_json import load_json"`
</verification>

<success_criteria>
- `uv run pyright --verifytypes jcx` reports 0 missing function annotations on public APIs
- All public modules import successfully
- Type hints are complete and accurate for functions in rs/, util/, sys/, time/, text/
</success_criteria>

<output>
After completion, create `.planning/phases/04-type-safety-documentation/04-02-SUMMARY.md`
</output>
