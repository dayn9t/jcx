---
phase: 04-type-safety-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/jcx/rs/*.py
  - src/jcx/util/*.py
  - src/jcx/sys/fs.py
  - src/jcx/sys/os_pkg.py
  - src/jcx/time/*.py
  - src/jcx/text/*.py
  - src/jcx/db/rdb/db.py
  - src/jcx/net/mqtt/publisher.py
  - src/jcx/net/mqtt/subscriber.py
autonomous: true
requirements:
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "All public functions have docstrings with parameter descriptions"
    - "Docstrings follow Google style format"
    - "Ruff D rules pass on public modules"
  artifacts:
    - path: "src/jcx/rs/"
      provides: "Result/Option types with docstrings"
    - path: "src/jcx/sys/fs.py"
      provides: "File system operations with docstrings"
    - path: "src/jcx/text/txt_json.py"
      provides: "JSON functions with docstrings"
  key_links:
    - from: "ruff"
      to: "docstring rules"
      via: "D-prefixed rules"
      pattern: "ruff check --select=D"
---

<objective>
Add docstrings to all public functions in the jcx library following Google style format. Focus on truly public APIs (not bin/ CLI tools, not test files, not internal _private functions).

Purpose: Provide inline documentation for IDE autocompletion and documentation generation.
Output: All public functions with comprehensive docstrings.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-type-safety-documentation/04-RESEARCH.md

<interfaces>
<!-- Functions requiring docstrings, organized by module -->

## Priority modules for docstrings:

### High priority (core public APIs):
1. `src/jcx/rs/` - Result/Option types
2. `src/jcx/sys/fs.py` - File system operations
3. `src/jcx/text/txt_json.py` - JSON handling
4. `src/jcx/text/txt_json5.py` - JSON5 handling

### Medium priority:
5. `src/jcx/time/clock_time.py` - Clock time parsing
6. `src/jcx/time/calendar_type.py` - Calendar types
7. `src/jcx/util/algo.py` - Algorithm utilities
8. `src/jcx/util/err.py` - Error utilities

### Lower priority (less frequently used):
9. `src/jcx/db/rdb/db.py` - Database operations
10. `src/jcx/net/mqtt/` - MQTT messaging

## Google-style docstring format:
```python
def function_name(param1: str, param2: int) -> bool:
    \"\"\"Brief description of function.

    Longer description if needed.

    Args:
        param1: Description of param1.
        param2: Description of param2.

    Returns:
        Description of return value.

    Raises:
        ValueError: When param1 is empty.
    \"\"\"
```

## Skip:
- `src/jcx/bin/` - CLI entry points (not public API)
- `*_test.py` files - Test files
- Functions starting with `_` - Internal functions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docstrings to rs/ module (Result/Option types)</name>
  <files>src/jcx/rs/*.py</files>
  <action>
    Add Google-style docstrings to all public functions and classes in the rs/ module.

    Focus on:
    - Result class methods (is_ok, is_err, unwrap, unwrap_or, expect)
    - Option class methods (is_some, is_none, unwrap, unwrap_or, expect)
    - Helper functions (Ok, Err, Some, Nothing)

    Example:
    ```python
    def unwrap(self) -> T:
        \"\"\"Return the contained Ok value.

        Returns:
            The contained value if Ok.

        Raises:
            UnwrapError: If this is an Err variant.
        \"\"\"
    ```
  </action>
  <verify>
    <automated>uv run ruff check src/jcx/rs/ --select=D --ignore=D100,D104 2>&1 | grep -c "D103\|D102" || echo "0"</automated>
  </verify>
  <done>All public functions in rs/ module have docstrings</done>
</task>

<task type="auto">
  <name>Task 2: Add docstrings to sys/fs.py and text modules</name>
  <files>src/jcx/sys/fs.py, src/jcx/sys/os_pkg.py, src/jcx/text/txt_json.py, src/jcx/text/txt_json5.py, src/jcx/text/io.py</files>
  <action>
    Add Google-style docstrings to public functions:

    **src/jcx/sys/fs.py** - Key functions:
    - `files_in(dir_path, suffix)` - List files with suffix
    - `find_first(dir_path, pattern)` - Find first matching file
    - `remake_dir(path)` - Recreate directory
    - `replace_in_file(path, old, new)` - Replace text in file

    **src/jcx/text/txt_json.py** - Key functions:
    - `load_json(path, cls)` - Load JSON with type
    - `save_json(obj, path, pretty)` - Save object as JSON
    - `to_json(obj)` - Convert to JSON string

    Example:
    ```python
    def load_json(path: Path, cls: type[T]) -> Result[T, str]:
        \"\"\"Load JSON file and parse into specified type.

        Args:
            path: Path to JSON file.
            cls: Pydantic model class to parse into.

        Returns:
            Result containing parsed object or error message.
        \"\"\"
    ```
  </action>
  <verify>
    <automated>uv run ruff check src/jcx/sys/ src/jcx/text/ --select=D --ignore=D100,D104 2>&1 | grep -c "D103\|D102" || echo "0"</automated>
  </verify>
  <done>All public functions in sys/ and text/ modules have docstrings</done>
</task>

<task type="auto">
  <name>Task 3: Add docstrings to time and util modules</name>
  <files>src/jcx/time/clock_time.py, src/jcx/time/calendar_type.py, src/jcx/util/algo.py, src/jcx/util/err.py, src/jcx/util/logging_config.py</files>
  <action>
    Add Google-style docstrings to public functions and classes:

    **src/jcx/time/clock_time.py**:
    - `ClockTime` class and methods
    - `to_clock_time()` function

    **src/jcx/time/calendar_type.py**:
    - `CalendarTrigger` class
    - `ClockPeriod` class

    **src/jcx/util/algo.py**:
    - Public utility functions

    **src/jcx/util/err.py**:
    - Error handling utilities

    **src/jcx/util/logging_config.py**:
    - `setup_logging()` function
  </action>
  <verify>
    <automated>uv run ruff check src/jcx/time/ src/jcx/util/ --select=D --ignore=D100,D104 2>&1 | grep -c "D103\|D102" || echo "0"</automated>
  </verify>
  <done>All public functions in time/ and util/ modules have docstrings</done>
</task>

</tasks>

<verification>
1. Run `uv run ruff check src/jcx/ --select=D --ignore=D100,D104` - should show minimal D103/D102 errors for public functions
2. Verify docstrings are Google-style format with Args/Returns sections
3. Spot check 5-10 functions for quality docstrings
</verification>

<success_criteria>
- All public functions in rs/, sys/, text/, time/, util/ have docstrings
- Docstrings follow Google style format
- Ruff D rules pass with minimal warnings (only internal/private functions may lack docstrings)
- Parameter descriptions included for functions with parameters
</success_criteria>

<output>
After completion, create `.planning/phases/04-type-safety-documentation/04-04-SUMMARY.md`
</output>
