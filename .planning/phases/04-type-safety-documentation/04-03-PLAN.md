---
phase: 04-type-safety-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-03
user_setup: []

must_haves:
  truths:
    - "README contains usage examples for each major module"
    - "Environment variables are documented with cross-reference to .env.example"
    - "Installation and quick start instructions are present"
  artifacts:
    - path: "README.md"
      provides: "Project documentation with usage examples"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "cross-reference"
      pattern: "See .env.example"
---

<objective>
Update README.md with comprehensive usage examples for each major module, installation instructions, and environment variable documentation.

Purpose: Provide clear documentation for users to understand and use the jcx library.
Output: Complete README.md with examples and references.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-type-safety-documentation/04-RESEARCH.md

<interfaces>
<!-- Current README content and structure -->

## Current README.md (minimal):
```markdown
# JCX

一些基础功能库。

## TODO

- 通过单元测试, 因为改用 pydantic 导致大量问题
- python稀烂的泛型

## 打包

\`\`\`bash
nuitka --job=8  --onefile --standalone --follow-imports --output-dir=dist src/jcx/bin/cx_task.py
\`\`\`
```

## .env.example already comprehensive with:
- Logging configuration (LOG_LEVEL, LOG_FORMAT)
- Database configuration (DATABASE_URL)
- Redis configuration (REDIS_URL)
- MQTT configuration (MQTT_URL, MQTT_CLIENT_ID)
- HTTP configuration (HTTP_PROXY, HTTPS_PROXY)
- Development settings (DEBUG, PYTHONPATH)

## Module structure for examples:
1. rs/ - Result/Option types
2. sys/fs.py - File system operations
3. time/ - Time utilities
4. text/ - JSON handling
5. util/ - Utility functions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with complete documentation</name>
  <files>README.md</files>
  <action>
    Replace the current minimal README.md with comprehensive documentation:

    ```markdown
    # JCX

    A Python utility library providing Result/Option types, file operations, time utilities, JSON handling, and more.

    ## Installation

    ```bash
    # Using uv (recommended)
    uv add jcx

    # Using pip
    pip install jcx
    ```

    ## Quick Start

    ```python
    from jcx.rs import Result, Ok, Err
    from jcx.sys.fs import files_in, remake_dir
    from jcx.text.txt_json import load_json, save_json
    ```

    ## Modules

    ### Result/Option Types (`jcx.rs`)

    Rust-inspired error handling with Result and Option types (powered by rustshed):

    ```python
    from jcx.rs import Result, Ok, Err, Option, Some, Nothing

    def divide(a: int, b: int) -> Result[float, str]:
        if b == 0:
            return Err("Division by zero")
        return Ok(a / b)

    result = divide(10, 2)
    if result.is_ok():
        print(f"Result: {result.unwrap()}")  # Result: 5.0
    ```

    ### File System Operations (`jcx.sys.fs`)

    ```python
    from pathlib import Path
    from jcx.sys.fs import files_in, find_first, remake_dir

    # Get all JSON files in a directory
    json_files = files_in("/data", ".json")

    # Find first matching file recursively
    result = find_first("/project", "*.toml")
    if result.is_ok():
        print(f"Found: {result.unwrap()}")

    # Recreate a clean directory
    work_dir = remake_dir("/tmp/workspace")
    ```

    ### Time Utilities (`jcx.time`)

    ```python
    from jcx.time.clock_time import ClockTime, to_clock_time
    from arrow import Arrow

    # Parse time from string
    time = ClockTime.parse("14:30:00")
    if time.is_some():
        print(f"Parsed: {time.unwrap()}")

    # Convert various time formats
    ct = to_clock_time(Arrow.now())
    ```

    ### JSON Handling (`jcx.text.txt_json`)

    ```python
    from jcx.text.txt_json import load_json, save_json, to_json
    from pydantic import BaseModel

    class Config(BaseModel):
        name: str
        value: int

    # Load configuration with type safety
    result = load_json("config.json", Config)
    if result.is_ok():
        config = result.unwrap()

    # Save with pretty formatting
    save_json(config, "output.json", pretty=True)
    ```

    ## Environment Variables

    See [.env.example](.env.example) for all configurable options:

    | Variable | Description | Default |
    |----------|-------------|---------|
    | `LOG_LEVEL` | Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL) | INFO |
    | `LOG_FORMAT` | Output format (json or text) | text |
    | `DATABASE_URL` | PostgreSQL connection URL | - |
    | `REDIS_URL` | Redis connection URL | - |
    | `MQTT_URL` | MQTT broker URL | - |

    ## Development

    ```bash
    # Install dev dependencies
    uv sync --dev

    # Run tests
    uv run pytest

    # Run type checking
    uv run pyright src/jcx

    # Format code
    uv run ruff format .
    ```

    ## License

    MIT
    ```

    This replaces the TODO section with actual documentation and removes the outdated nuitka packaging section (can be added back if needed).
  </action>
  <verify>
    <automated>grep -q "## Modules" /home/jiang/py/jcx/README.md && grep -q "jcx.rs" /home/jiang/py/jcx/README.md</automated>
  </verify>
  <done>README.md contains installation, quick start, module examples, and environment variable documentation</done>
</task>

</tasks>

<verification>
1. README.md has sections: Installation, Quick Start, Modules, Environment Variables, Development
2. Each major module (rs, sys/fs, time, text) has usage examples
3. Environment variables reference .env.example
4. README renders correctly in markdown viewers
</verification>

<success_criteria>
- README.md has complete installation instructions
- Usage examples for rs/, sys/fs.py, time/, text/ modules
- Environment variable documentation with .env.example reference
- Development section with test and type checking commands
- README is at least 100 lines of comprehensive documentation
</success_criteria>

<output>
After completion, create `.planning/phases/04-type-safety-documentation/04-03-SUMMARY.md`
</output>
