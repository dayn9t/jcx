---
phase: 04-type-safety-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/jcx/bin/cx_cvt.py
  - src/jcx/net/mqtt/publisher.py
  - src/jcx/net/mqtt/subscriber.py
  - src/jcx/db/rdb/mutithread.py
  - src/jcx/api/command.py
  - src/jcx/api/_dao_list.py
  - src/jcx/api/_dao_item.py
  - src/jcx/time/clock_time.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
user_setup: []

must_haves:
  truths:
    - "pyright runs without errors on the jcx package"
    - "type:ignore comments removed where libraries have built-in types"
    - "type:ignore comments documented where libraries lack type stubs"
  artifacts:
    - path: "pyproject.toml"
      provides: "pyright configuration and dependencies"
      contains: "pyright"
    - path: "src/jcx/net/mqtt/*.py"
      provides: "MQTT modules without type:ignore for paho-mqtt"
    - path: "src/jcx/db/rdb/mutithread.py"
      provides: "Redis module without type:ignore"
  key_links:
    - from: "pyproject.toml"
      to: "pyright type checker"
      via: "dev dependency"
      pattern: "pyright"
---

<objective>
Clean up all type:ignore comments by removing unnecessary ones (libraries now have built-in types) and documenting necessary ones (libraries without stubs). Add pyright to dev dependencies for type checking.

Purpose: Achieve clean type checking with pyright and proper documentation of intentional type:ignore usage.
Output: pyright configuration, cleaned up type annotations.
</objective>

<execution_context>
@/home/jiang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-type-safety-documentation/04-RESEARCH.md

<interfaces>
<!-- Key interfaces from research and codebase -->

## Current type:ignore locations (9 in source):

1. `src/jcx/bin/cx_cvt.py:18` - argparse attribute access (fixable)
2. `src/jcx/bin/cx_cvt.py:27` - argparse attribute access (fixable)
3. `src/jcx/net/mqtt/publisher.py:4` - paho-mqtt (REMOVE - has built-in types)
4. `src/jcx/net/mqtt/subscriber.py:5` - paho-mqtt (REMOVE - has built-in types)
5. `src/jcx/db/rdb/mutithread.py:3` - redis (REMOVE - has built-in types)
6. `src/jcx/api/command.py:4` - flask-restx (document - no stubs)
7. `src/jcx/api/_dao_list.py:5` - flask-restx (document - no stubs)
8. `src/jcx/api/_dao_item.py:6` - flask-restx (document - no stubs)
9. `src/jcx/time/clock_time.py:4` - parse library (document - no stubs)

## Library type support status:
- paho-mqtt 2.1.0: Built-in types since 2.0.0
- redis 7.1.0: Built-in types since 5.0.0
- flask-restx: No official stubs available
- parse 1.20.2: No official stubs available

## types-paho-mqtt in pyproject.toml is REDUNDANT - remove it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pyright configuration and remove redundant stub package</name>
  <files>pyproject.toml</files>
  <action>
    1. Add pyright to dev dependencies: `uv add --dev pyright`
    2. Remove types-paho-mqtt from dependencies (it's redundant since paho-mqtt 2.1.0 has built-in types)
    3. Add pyright configuration section:

    ```toml
    [tool.pyright]
    typeCheckingMode = "basic"
    pythonVersion = "3.12"
    include = ["src"]
    exclude = ["**/__pycache__"]
    reportMissingImports = true
    reportMissingTypeStubs = false
    ```
  </action>
  <verify>
    <automated>grep -q "pyright" /home/jiang/py/jcx/pyproject.toml && ! grep -q "types-paho-mqtt" /home/jiang/py/jcx/pyproject.toml</automated>
  </verify>
  <done>pyright in dev dependencies, types-paho-mqtt removed, pyright config added</done>
</task>

<task type="auto">
  <name>Task 2: Remove type:ignore for paho-mqtt and redis</name>
  <files>src/jcx/net/mqtt/publisher.py, src/jcx/net/mqtt/subscriber.py, src/jcx/db/rdb/mutithread.py</files>
  <action>
    Remove `# type: ignore` comments from imports in these files:

    1. `src/jcx/net/mqtt/publisher.py:4`:
       - Change: `from paho.mqtt import publish  # type: ignore`
       - To: `from paho.mqtt import publish`

    2. `src/jcx/net/mqtt/subscriber.py:5`:
       - Change: `import paho.mqtt.client as mqtt  # type: ignore`
       - To: `import paho.mqtt.client as mqtt`

    3. `src/jcx/db/rdb/mutithread.py:3`:
       - Change: `import redis  # type: ignore`
       - To: `import redis`

    These libraries now have built-in type hints (paho-mqtt since 2.0.0, redis since 5.0.0).
  </action>
  <verify>
    <automated>! grep -q "# type: ignore" /home/jiang/py/jcx/src/jcx/net/mqtt/publisher.py /home/jiang/py/jcx/src/jcx/net/mqtt/subscriber.py /home/jiang/py/jcx/src/jcx/db/rdb/mutithread.py</automated>
  </verify>
  <done>No type:ignore comments in paho-mqtt or redis imports</done>
</task>

<task type="auto">
  <name>Task 3: Fix argparse type annotations in cx_cvt.py</name>
  <files>src/jcx/bin/cx_cvt.py</files>
  <action>
    Replace type:ignore with proper type annotations using cast:

    1. Line 18: Change `src_file: Path = opt.file  # type: ignore`
       To: `src_file = cast(Path, opt.file)`

    2. Line 27: Change `dst_file: Path = src_file.with_suffix(opt.ext)  # type: ignore`
       To: `dst_file = src_file.with_suffix(cast(str, opt.ext))`

    3. Add import: `from typing import cast`

    The argparse.Namespace attributes are dynamically typed, so cast is appropriate here.
  </action>
  <verify>
    <automated>! grep -q "# type: ignore" /home/jiang/py/jcx/src/jcx/bin/cx_cvt.py</automated>
  </verify>
  <done>cx_cvt.py has no type:ignore comments, uses cast for argparse attributes</done>
</task>

<task type="auto">
  <name>Task 4: Document type:ignore for libraries without stubs</name>
  <files>src/jcx/api/command.py, src/jcx/api/_dao_list.py, src/jcx/api/_dao_item.py, src/jcx/time/clock_time.py</files>
  <action>
    Add descriptive comments explaining why type:ignore is necessary:

    1. `src/jcx/api/command.py` - Before import add comment:
       ```python
       # flask-restx has no type stubs - type: ignore[import]
       from flask_restx import Api, fields  # type: ignore[import]
       ```

    2. `src/jcx/api/_dao_list.py` - Before import add comment:
       ```python
       # flask-restx has no type stubs - type: ignore[import]
       from flask_restx import Resource  # type: ignore[import]
       ```

    3. `src/jcx/api/_dao_item.py` - Before import add comment:
       ```python
       # flask-restx has no type stubs - type: ignore[import]
       from flask_restx import Resource  # type: ignore[import]
       ```

    4. `src/jcx/time/clock_time.py` - Before import add comment:
       ```python
       # parse library (v1.20.2) has no type stubs available
       from parse import parse  # type: ignore[import]
       ```

    Use specific error code `[import]` or `[import-untyped]` for better clarity.
  </action>
  <verify>
    <automated>grep -q "# flask-restx has no type stubs" /home/jiang/py/jcx/src/jcx/api/command.py</automated>
  </verify>
  <done>All remaining type:ignore comments have descriptive documentation</done>
</task>

</tasks>

<verification>
1. Run `uv run pyright src/jcx` - should complete with no errors
2. Verify no type:ignore remains for paho-mqtt, redis
3. Verify type:ignore for flask-restx and parse are documented
</verification>

<success_criteria>
- pyright added to dev dependencies and configured
- types-paho-mqtt removed from dependencies
- 4 type:ignore comments removed (paho-mqtt, redis, argparse)
- 4 type:ignore comments documented (flask-restx x3, parse x1)
- `uv run pyright src/jcx` passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-type-safety-documentation/04-01-SUMMARY.md`
</output>
