[project]
name = "jcx"
version = "0.4.8"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dayn9t", email = "dayn9t@gmail.com" }
]
requires-python = "~=3.12.0"
dependencies = [
    "rustshed",
    "pydantic",
    "pydantic-extra-types",
    "typer",
    "arrow",
    "loguru",
    "arrow",
    "paho-mqtt",
    "parse",
    "redis",
    "requests",
    "sh",
    "interval",
    "numpy",
    "types-paho-mqtt",
]

[project.scripts]
#cx_rename = "src/jcx/bin/cx_rename.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
required-environments = ["manylinux_2_17_x86_64", "manylinux2014_x86_64"]
